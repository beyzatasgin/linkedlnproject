## Frontend environment variables (Vite)
#
# By default the frontend uses "/api" (Vite proxy) so you usually don't need this.
# If you want to hit Django directly (without proxy), set:
# VITE_API_URL=http://127.0.0.1:8000/api
#
VITE_API_URL=


